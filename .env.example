# =============================================================================
# CONFIGURATION D'ENVIRONNEMENT - EXEMPLE
# =============================================================================
# Copiez ce fichier vers .env et remplissez les valeurs appropri�es
# Ne commitez jamais le fichier .env dans votre d�p�t Git !

# =============================================================================
# ENVIRONNEMENT
# =============================================================================
NODE_ENV=development

# =============================================================================
# BASE DE DONN�ES
# =============================================================================
# URL de connexion PostgreSQL (Neon ou autre fournisseur)
# Format: postgresql://username:password@host:port/database?sslmode=require
DATABASE_URL="postgresql://username:password@host:port/database?sslmode=require"

# =============================================================================
# AUTHENTIFICATION (NextAuth.js)
# =============================================================================
# Cl� secr�te pour NextAuth.js (minimum 16 caract�res)
# G�n�rez une cl� s�curis�e avec: openssl rand -base64 32
NEXTAUTH_SECRET="your-secret-key-here-minimum-16-chars"

# URL de base de votre application (pour NextAuth.js)
NEXTAUTH_URL="http://localhost:3000"

# =============================================================================
# EMAIL (Optionnel)
# =============================================================================
# Configuration du serveur SMTP pour l'envoi d'emails
# Format: smtp://username:password@smtp.example.com:587
EMAIL_SERVER="smtp://username:password@smtp.example.com:587"

# Adresse email d'exp�dition
EMAIL_FROM="noreply@example.com"

# =============================================================================
# REDIS (Optionnel)
# =============================================================================
# URL de connexion Redis standard (pour Socket.IO)
REDIS_STANDARD_URL="redis://localhost:6379"

# URL de connexion Redis (pour la limitation de taux et autres)
REDIS_URL="redis://localhost:6379"

# =============================================================================
# UPSTASH REDIS (Optionnel - pour la limitation de taux)
# =============================================================================
# URL REST Upstash Redis
UPSTASH_REDIS_REST_URL="https://your-redis-url.upstash.io"

# Token d'authentification Upstash Redis
UPSTASH_REDIS_REST_TOKEN="your-upstash-token"

# =============================================================================
# NOTIFICATIONS PUSH (Optionnel)
# =============================================================================
# Cl� API OneSignal pour les notifications push
ONESIGNAL_API_KEY="your-onesignal-api-key"

# =============================================================================
# STOCKAGE S3 (Optionnel - pour les images des salles)
# =============================================================================
# Cl� d'acc�s AWS S3 ou compatible
S3_ACCESS_KEY="your-s3-access-key"

# Cl� secr�te AWS S3 ou compatible
S3_SECRET_KEY="your-s3-secret-key"

# URL du bucket S3 (avec le nom du bucket)
S3_BUCKET_URL="https://your-bucket-name.s3.amazonaws.com"
